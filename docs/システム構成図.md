```mermaid
graph TD
    %% スタイル定義 (Style Definitions)
    classDef frontend fill:#61dafb,stroke:#333,stroke-width:2px,color:#000;
    classDef backend fill:#009688,stroke:#333,stroke-width:2px,color:#fff;
    classDef ai fill:#ff9800,stroke:#333,stroke-width:2px,color:#fff;
    classDef database fill:#336791,stroke:#333,stroke-width:2px,color:#fff;
    classDef line fill:#00c300,stroke:#333,stroke-width:2px,color:#fff;

    %% 各レイヤーの定義 (System Layers)
    subgraph client_layer ["クライアント / エッジ層 (Vercel)"]
        A[React.js Webアプリ]
    end

    subgraph cloud_layer ["クラウドサーバー層 (Docker on Render)"]
        B[FastAPI バックエンドコア]
        C[InsightFace AIコア]
    end

    subgraph db_layer ["データベース層 (Render)"]
        D[(PostgreSQL データベース)]
    end

    subgraph ext_layer ["外部サービス連携層"]
        E[メール]
        F[従業員のスマートフォン]
    end

    %% データフロー (Workflow & Data Flow)
    A -->|1. HTTPS POSTで生画像を送信| B
    B -->|2. 内部ライブラリを呼び出し| C
    C -->|3. 512次元ベクトルを返却| B
    B -->|4. ベクトル照合 & ログ保存| D
    D -->|5. 認証結果を返却| B
    B -->|6. 画面へ結果をフィードバック| A
    B -.->|7. メール送信| E
    E -.->|8. リアルタイムプッシュ通知を送信| F

    %% スタイルの適用 (Apply Styles)
    class A frontend;
    class B backend;
    class C ai;
    class D database;
    class E,F line;
